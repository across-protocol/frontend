import React from "react";
import styled from "@emotion/styled";
import { SecondaryButton } from "../BaseButton";

const Wallet: React.FC = () => {
  return <SecondaryButton>Connect Wallet</SecondaryButton>;
};

export default Wallet;
