import {
  isValidAddress,
  getAddress,
  noContractCode,
  getCode,
} from "../address";
import { ethers } from "ethers";

const VALID_ADDRESS = "0x04Fa0d235C4abf4BcF4787aF4CF447DE572eF828";
describe("#isValidAddress", () => {
  test("isValidAddress returns true with a valid ETH address", () => {
    expect(isValidAddress(VALID_ADDRESS)).toBeTruthy();
  });
});
