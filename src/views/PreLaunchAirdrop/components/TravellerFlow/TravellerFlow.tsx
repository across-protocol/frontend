import { Wrapper, HeroBlock, StyledRotate } from "./TravellerFlow.styles";
const TravellerFlow = () => {
  return (
    <Wrapper>
      <HeroBlock>
        <StyledRotate />
      </HeroBlock>
    </Wrapper>
  );
};

export default TravellerFlow;
